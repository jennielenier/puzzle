const size = {
    mobile: '576px',
    tablet: '768px',
    desktop: '1200px'
}

export const device = {
    mobile: `(max-width: ${size.mobile})`,
    tablet: `(max-width: ${size.tablet})`,
    desktop: `(max-width: ${size.desktop})`,
}

export const width = {
    sm: '540px',
    md: '750px',
    xl: '1140px',
}